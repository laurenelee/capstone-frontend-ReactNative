// import libs
import React from 'react';
import { Text } from 'react-native';

// make Component
// name it the same as file name
const Header = () => {
  return <Text> Volunteer Agencies </Text>;
};

// make component available to other parts of app
export default Header;
